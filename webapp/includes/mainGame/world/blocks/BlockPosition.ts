export default class BlockPosition {
	x: number;
	y: number;
	z: number;
	zIndex: number;
	constructor(x: number, y: number, z: number, zIndex: number) {
		this.x = x;
		this.y = y;
		this.z = z;
		this.zIndex = zIndex;
	}
}
